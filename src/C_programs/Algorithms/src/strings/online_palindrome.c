#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <error.h>


/** Given a stream of characters (characters are received one by one),
 * write a function that prints ‘Yes’ if a character makes the complete
 * string palindrome, else prints ‘No’.*/

void main() {








} 
