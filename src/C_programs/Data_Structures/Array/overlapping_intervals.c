#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <error.h>


/** Check if any two intervals overlap among a given set of intervals
 * An interval is represented as a combination of start time and end
 * time. Given a set of intervals, check if any two intervals overlap.
 * */

/** Solution: Sort the array of intervals with starting interval time as
 * key, then loop through the list to check if any of the starting
 * intervaltime is less than previous ending time, if so theres an
 * overlap */

void main() {








} 
